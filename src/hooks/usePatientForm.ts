/**
 * usePatientForm.ts — hook for form state in PatientFormPage.
 * OWNERSHIP: Person C.
 *
 * Stub — implementation to be added during Phase A.
 */

// TODO (Person C): implement with React Hook Form + zodResolver + patientSchema
export function usePatientForm() {
  return {}
}
